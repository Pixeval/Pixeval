<controls:EnhancedPage
    x:Class="Pixeval.Pages.Capability.FollowingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Pixeval.Controls"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    ClearCacheAfterNavigation="True"
    mc:Ignorable="d">
    <controls:EnhancedPage.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </controls:EnhancedPage.Resources>
    <Grid>
        <controls:AdvancedItemsView
            IsItemInvokedEnabled="True"
            ItemInvoked="IllustratorItemsView_OnItemInvoked"
            ItemsSource="{x:Bind _viewModel.DataProvider.View}"
            LayoutType="Grid"
            MinItemWidth="300"
            SelectionMode="None">
            <controls:AdvancedItemsView.ItemTemplate>
                <DataTemplate x:DataType="controls:IllustratorItemViewModel">
                    <ItemContainer>
                        <controls:IllustratorItem
                            RequestTeachingTip="IllustratorItemOnRequestTeachingTip"
                            ViewModel="{x:Bind}"
                            ViewModelChanged="IllustratorItem_OnViewModelChanged" />
                    </ItemContainer>
                </DataTemplate>
            </controls:AdvancedItemsView.ItemTemplate>
        </controls:AdvancedItemsView>
        <StackPanel
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="20"
            Visibility="{x:Bind _viewModel.HasNoItems, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <Image
                Width="200"
                Height="200"
                Source="/Assets/Images/empty-box.png" />
            <TextBlock
                x:Uid="/FollowingsPage/ResultIsEmptyTextBlock"
                HorizontalAlignment="Center"
                FontSize="{StaticResource PixevalTitleFontSize}" />
        </StackPanel>
        <TeachingTip
            x:Name="FollowingsPageTeachingTip"
            x:Uid="/FollowingsPage/QrCodeTeachingTip"
            IsLightDismissEnabled="True">
            <TeachingTip.HeroContent>
                <Image />
            </TeachingTip.HeroContent>
        </TeachingTip>
    </Grid>
</controls:EnhancedPage>
