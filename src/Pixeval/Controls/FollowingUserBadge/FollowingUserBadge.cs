using Microsoft.UI.Xaml.Controls;

namespace Pixeval.Controls.FollowingUserBadge;

public sealed class FollowingUserBadge : Control
{
    public FollowingUserBadge()
    {
        DefaultStyleKey = typeof(FollowingUserBadge);
    }
}