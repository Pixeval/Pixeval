<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Pixeval.Controls.PremiumUserBadge">
    <Style TargetType="local:PremiumUserBadge">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:PremiumUserBadge">
                    <Grid
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}"
                        VerticalAlignment="Center"
                        Background="#FD9E16"
                        CornerRadius="{ThemeResource ControlCornerRadius}">
                        <TextBlock
                            Margin="2"
                            FontSize="7"
                            Foreground="White"
                            Text="Premium" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
