<UserControl x:Class="Pixeval.Controls.Setting.UI.UserControls.ActionableSettingEntry"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:card="using:Pixeval.Controls.Card"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:userControls="using:Pixeval.Controls.Setting.UI.UserControls"
             MinHeight="48"
             mc:Ignorable="d">

    <card:CardControl>
        <Grid Padding="15,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <userControls:SettingEntryHeader x:Name="EntryHeader"
                                             Grid.Column="0"
                                             HorizontalAlignment="Stretch"
                                             VerticalAlignment="Center" />
            <ContentPresenter x:Name="ActionContainer"
                              Grid.Column="1"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center" />
        </Grid>
    </card:CardControl>
</UserControl>
