<controls:SettingsExpander
    x:Class="Pixeval.Controls.Settings.DomainFrontingSettingsExpander"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fluent="using:FluentIcons.WinUI"
    xmlns:local="using:Pixeval.Settings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winUi="using:AutoSettingsPage.WinUI"
    Description="{x:Bind winUi:SettingsEntryHelper.DescriptionControl(Entry)}"
    Header="{x:Bind Entry.Header}"
    Tag="{x:Bind Entry.Token}"
    mc:Ignorable="d">
    <controls:SettingsExpander.HeaderIcon>
        <fluent:SymbolIcon Symbol="{x:Bind Entry.Icon}" />
    </controls:SettingsExpander.HeaderIcon>
    <ToggleSwitch IsOn="{x:Bind Entry.Value, Mode=TwoWay}" />
    <controls:SettingsExpander.Items>
        <controls:SettingsCard
            HorizontalContentAlignment="Left"
            ContentAlignment="Vertical"
            Description="{x:Bind winUi:SettingsEntryHelper.DescriptionControl(FirstEntry)}"
            Header="{x:Bind FirstEntry.Header}"
            Tag="{x:Bind FirstEntry.Token}">
            <controls:SettingsCard.HeaderIcon>
                <fluent:SymbolIcon Symbol="{x:Bind FirstEntry.Icon}" />
            </controls:SettingsCard.HeaderIcon>
            <controls:WrapPanel
                x:Name="WrapPanel"
                HorizontalSpacing="10"
                VerticalSpacing="10" />
        </controls:SettingsCard>
    </controls:SettingsExpander.Items>
</controls:SettingsExpander>
